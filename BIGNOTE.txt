Have a centralized ALL_INCLUDE, that includes all the project .hs, and then that is referenced in all the files for getting project variables because the ifndefs make everything only once, the centralized hub ensures that it does that, since all the calls are made by the preprocessor when compiling, it has no effect other than guaranteeing that the output code will be as small as possible